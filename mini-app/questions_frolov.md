# Вопросы Б.В. Фролову — стык фронт↔бэк

> Юра, Mini App (фронт) — на мне. От тебя нужен бэк. Накидал точки стыка — ответь по пунктам, и я начну вкручивать `fetch` вместо текущей localStorage-заглушки.

## 1. API — общая форма

1. **Базовый URL** API на проде и стейдже? (например `https://api.cyberdef.ru` / `https://staging...`).
2. **Аутентификация**. Я планирую слать заголовок `X-Telegram-Init-Data` со значением `Telegram.WebApp.initData`. Ты на бэке валидируешь подпись через `bot_token`. Подойдёт? Если нет — какой механизм?
3. **Формат ошибок**: `{ "error": "code", "message": "..." }` устроит? Какие коды нужны?
4. **CORS**: разрешишь origin Mini App-хостинга (см. п.4 ниже)?

## 2. Эндпоинты (минимальный набор для MVP)

Предлагаю такие — скажи, что меняем:

| Метод | Путь | Что делает | Что отдаёт |
|---|---|---|---|
| `GET`  | `/me` | Профиль текущего пользователя (по initData) | `{ id, first_name, username, total_score, rank }` |
| `POST` | `/results` | Записать результат игры | `{ ok: true, new_total, new_rank }` |
| `GET`  | `/leaderboard/:gameId` | Топ по конкретной игре | `{ entries: [{ user, score, ts }] }` |
| `GET`  | `/leaderboard/total` | Общий топ | то же |

**Тело `POST /results`**:
```json
{
  "gameId": "passwords",
  "score": 120,
  "stars": 3,
  "duration_ms": 87000,
  "meta": { "level": 2, "hints_used": 1 }
}
```

Подходит? Что-то добавишь/уберёшь?

## 3. БД — что хранится на твоей стороне

- Пользователи: `tg_id`, имя, username, дата первого входа, всего очков, ранг.
- Результаты: `user_id`, `game_id`, `score`, `stars`, `duration_ms`, `ts`, `meta jsonb`.
- (Опц.) Достижения / бейджи — если будем делать в этапе 3.

Это норм или своя схема?

## 4. Хостинг Mini App

Где деплоим фронт? Варианты:
- **Vercel / Netlify** (бесплатно, статический хостинг, HTTPS из коробки) — я могу поднять сам.
- **Твой сервер** — раздадим через nginx по поддомену (напр. `app.cyberdef.ru`).
- **GitHub Pages** — тоже вариант, но домен `.github.io` ограничения.

Где удобнее? Если на твоём сервере — нужен поддомен с валидным TLS-сертификатом (Telegram требует HTTPS).

## 5. Bot / запуск Mini App

- Какой `username` у бота?
- Уже зарегистрировал Mini App через `@BotFather` (`/newapp`)? Если да — какой short name?
- Кнопка запуска: меню-кнопка бота или inline-кнопка с `web_app`?

## 6. Дедлайны и формальности

- **Когда защита этапа 3?** (дата) — чтобы рассчитать темп.
- Кто отвечает за Главу 3 отчёта в части серверной интеграции? Если ты — какой объём и формат от тебя нужен?
- Презентация защиты — общая или у каждого своя?

## 7. Брендинг (мелочи)

- Финальное название Mini App? У меня пока рабочее «Кибер-Защитник» — годится или меняем?
- Нужен ли персонаж-наставник (тот, что показывает подсказки)? Если да — есть концепт-арт?

---

**Срочное (без чего я не могу двигаться сейчас)**: пункты 1, 2, 4, 6.
**Можно ответить позже**: 3, 5, 7.

Готов созвониться на 15 минут, если так быстрее.
